# WEBPACK-JS-SCSS--portfolio

`<npm install>` - to install whole webpack

`<npm start>` - to start live server

`<npm run prod>` - to build prod version 

In src folder you can find whole code that compile to 'docs' folder
